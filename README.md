# WEEK6 TSPのプログラムの改良
コードはmainブランチのweek6_hw.py
※生成AIの使用がこの回は許可されている

## 前回の時点で実装されていたヒューリスティック
・貪欲法  
・2-OPT  
・焼きなまし法  

## 新しく実装したヒューリスティック
・OR-OPT  
・ダブルブリッジ法  
・領域分割  

## 元からあった関数の変更点
1. 前回のoffice hourで指摘していただいた点の可能な限りの修正
   毎回distanceを計算するのではなく、全都市から全都市への距離を計算したテーブルを持っておくように変更
   => distance()を削除し、build_distance_table()を作成した。

3. build_distance_table()でmath.hypot()を用いることでオーバーフローを防止

4. greedy_algorithmで、visitedではなく、辿ったことがない都市のセット(unvisited)を用意
   => 計算量は同じだが、速度的には倍になることが期待される。

5. 焼きなまし法を、受け取った文字列の長さに応じて繰り返し行うように変更

6. そのほか様々な微調整

## 今回から新しく実装した関数
1. Or-opt
